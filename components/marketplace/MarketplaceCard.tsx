'use client';
import Link from 'next/link';import { motion } from 'framer-motion';import type { ComponentItem } from '@/lib/types';import { ComponentPreview } from './ComponentPreview';
export function MarketplaceCard({item}:{item:ComponentItem}){return <motion.article whileHover={{y:-6,scale:1.015}} className="group overflow-hidden rounded-[28px] border border-white/10 bg-[#18181B]/80 shadow-2xl shadow-black/20 transition-colors hover:border-[#4F7CFF]/70"><div className="relative h-56"><ComponentPreview item={item}/><div className="absolute inset-0 flex items-center justify-center bg-black/40 opacity-0 backdrop-blur-sm transition-opacity group-hover:opacity-100"><Link className="rounded-full bg-white px-4 py-2 text-sm font-semibold text-black" href={`/marketplace/${item.slug}`}>Preview</Link></div></div><div className="space-y-3 p-5"><div className="flex items-start justify-between gap-3"><div><h3 className="font-semibold text-white">{item.name}</h3><p className="text-xs text-zinc-500">{item.category} / {item.subCategory}</p></div><span className="rounded-full border border-white/10 px-2 py-1 text-[11px] text-cyan-200">{item.priceType}</span></div><p className="line-clamp-2 text-sm text-zinc-400">{item.description}</p><div className="flex flex-wrap gap-2">{item.tags.slice(0,3).map(t=><span className="rounded-full bg-white/[.06] px-2 py-1 text-[11px] text-zinc-300" key={t}>{t}</span>)}</div><div className="grid grid-cols-2 gap-2 text-[11px] text-zinc-400"><span>Interaction: {item.interactionType.split(' ')[0]}</span><span>Motion: {item.motionType.split(' ')[0]}</span><span>Creator: {item.creator}</span><span>Responsive</span></div><div className="flex gap-2 opacity-100 md:opacity-0 md:transition-opacity md:group-hover:opacity-100"><Link href={`/marketplace/${item.slug}`} className="flex-1 rounded-xl bg-[#4F7CFF] px-3 py-2 text-center text-xs font-semibold text-white">Preview</Link><button className="rounded-xl border border-white/10 px-3 py-2 text-xs">Save</button><button className="rounded-xl border border-white/10 px-3 py-2 text-xs">Export</button></div></div></motion.article>}
