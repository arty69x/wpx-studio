import { marketplaceItems } from '@/data/marketplace';import { StructureTree } from '@/components/marketplace/StructureTree';
export function StructureEngine(){return <section id="structure" className="mx-auto max-w-7xl px-4 py-20"><div className="grid gap-8 lg:grid-cols-2"><div><h2 className="text-3xl font-semibold text-white">Locked structure engine</h2><p className="mt-4 text-zinc-400">Website → Page → Section → Container → Grid → Row → Column → Stack → Component → Node → Element → Micro Div. Sections own rhythm, containers own max width, grids own two-dimensional placement, and components own reusable behavior.</p></div><div className="rounded-3xl border border-white/10 bg-[#111214] p-4"><StructureTree node={marketplaceItems[0].structure}/></div></div></section>}
