'use client';
import { useMemo,useState } from 'react';import Link from 'next/link';import { marketplaceItems } from '@/data/marketplace';import { MarketplaceCard } from '@/components/marketplace/MarketplaceCard';import { CategoryTabs } from '@/components/marketplace/CategoryTabs';import { MarketplaceFilters } from '@/components/marketplace/MarketplaceFilters';
export default function MarketplacePage(){const [q,setQ]=useState('');const [cat,setCat]=useState('All');const [sub,setSub]=useState('All');const [sort,setSort]=useState('Featured');const items=useMemo(()=>marketplaceItems.filter(i=>(cat==='All'||i.category===cat)&&(sub==='All'||i.subCategory===sub)&&(`${i.name} ${i.category} ${i.subCategory} ${i.tags.join(' ')}`.toLowerCase().includes(q.toLowerCase()))).sort((a,b)=>sort==='Name'?a.name.localeCompare(b.name):0),[q,cat,sub,sort]);return <main className="min-h-screen bg-[#09090B] text-white"><nav className="sticky top-0 z-40 border-b border-white/10 bg-[#09090B]/80 backdrop-blur"><div className="mx-auto flex max-w-7xl items-center justify-between px-4 py-4"><Link href="/" className="font-semibold">WPX Studio</Link><Link href="/marketplace" className="text-sm text-zinc-400">Marketplace</Link></div></nav><section className="mx-auto max-w-7xl px-4 py-10"><div className="mb-8 rounded-[32px] border border-white/10 bg-[#111214] p-6"><p className="text-sm uppercase tracking-[.25em] text-[#45D6FF]">Component marketplace</p><h1 className="mt-3 text-4xl font-semibold">Browse production-ready UI components.</h1><div className="mt-6 flex flex-col gap-3 md:flex-row"><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Ask WPX to find a carousel, reveal, nav, or background..." className="min-h-12 flex-1 rounded-2xl border border-white/10 bg-black/30 px-4 outline-none focus:ring-2 focus:ring-[#4F7CFF]"/><select value={sort} onChange={e=>setSort(e.target.value)} className="rounded-2xl border border-white/10 bg-black/30 px-4"><option>Featured</option><option>Name</option></select></div></div><CategoryTabs category={cat} setCategory={setCat} sub={sub} setSub={setSub}/><div className="mt-8 grid gap-6 lg:grid-cols-[240px_1fr]"><MarketplaceFilters/><MarketplaceFilters mobile/><div className="grid gap-6 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4">{items.map(i=><MarketplaceCard key={i.id} item={i}/>)}</div></div></section></main>}
